[project]
name = "django-web"
version = "0.1.0"
requires-python = ">=3.11, <3.12"
dependencies = [
    "Django>=5.2.17,<5.3",
    "mysqlclient>=2.2",
    "python-dotenv>=1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-django>=4.9",
]
