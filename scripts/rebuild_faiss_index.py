def main() -> None:
    # 이 CLI는 기존 인덱스를 증분 수정하지 않고 전체 원본 문서로 새 버전을 빌드한다.
    # 새 인덱스와 metadata의 정합성을 검사한 후에만 활성 경로를 교체하고, 실패 시 기존
    # 검색 가능한 버전을 보존한다. 결과 버전은 캐시 무효화/키 계산에 사용될 수 있어야 한다.
    ...


if __name__ == "__main__":
    main()
