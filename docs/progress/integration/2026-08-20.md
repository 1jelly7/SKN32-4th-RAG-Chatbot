# 2026-08-20 Integration 진행 이력

- 역할: `integration`
- 타임존: `Asia/Seoul (KST)`
- 일일 요약: Django가 인증·계정을, FastAPI가 채팅·MCP 조율을 소유하는 코드 경계를 구현하고 현재 코드 기준으로 README·계약·운영 문서를 동기화했다. 구현 직후 `.venv` 전체 자동 테스트를 통과했고, 후속 정적 수정과 문서 최신화 뒤에는 테스트를 재실행하지 않았다.

## Session 1 — 11:43–11:43 KST | INT-AUTH Django·FastAPI 인증 경계 분리

- 작성자: integration-team
- 역할: `integration`
- 상태: In progress
- 브랜치: `codex/plan-fastapi-django`
- 이슈 / PR / 커밋 / 관련 링크: N/A

### Goal

- 구조 분리 계획에 따라 Django 인증 서비스를 추가하고 FastAPI의 활성 account DB 접근을 제거한다.

### Scope

- 포함: Django 사용자·Admin·migration, legacy scrypt 이관, 인증 API, 내부 인증 확인 gateway, CSRF UI 연동, 계약 테스트와 문서 동기화
- 제외: 실제 의존성 설치, 테스트·migration 실행, reverse proxy/Ingress 구성, 운영 전환, legacy 코드 삭제

### Changes

- `django_app/`에 `AbstractUser` 기반 계정, Django Admin, 공개 인증 API와 내부 세션 확인 API를 추가했다.
- 기존 `accounts` 행의 ID·역할·상태·scrypt 해시를 보존하는 migration과 이관 감사 명령을 추가했다.
- FastAPI 보호 API가 Django 내부 인증 확인 결과만 사용하도록 gateway를 연결하고 기존 인증 라우터 등록을 해제했다.
- CSRF 토큰 발급·전달과 로그인 후 토큰 회전을 UI에 반영했다.
- `BOTH` 계약을 실제 병렬 fan-out 코드에 맞춰 정본 문서에서 통일했다.

### Files Changed / Artifacts

- `django_app/`: Django 인증·계정 서비스와 migration
- `app/auth/gateway.py`, `app/auth/dependencies.py`: 서비스 간 인증 확인 경계
- `shared/auth_policy.py`: Django·FastAPI 공통 역할 정책
- `tests/django/`, `tests/unit/test_auth_gateway.py`: 신규 계약 테스트
- `docs/django-fastapi-separation-plan.md`: 단계별 구현 진행도와 결정 기록

### Verification

- 변경된 Python 파일 AST 구문 파싱: 통과
- stale 활성 인증 참조와 계약 문구 검색: 활성 FastAPI 경로의 직접 account DB 접근 없음 확인
- `git diff --check`: 통과
- `.venv\Scripts\python.exe django_app\manage.py check --settings=django_app.config.test_settings`: 이상 없음
- `.venv\Scripts\python.exe django_app\manage.py makemigrations --check --dry-run --settings=django_app.config.test_settings`: 변경 없음
- `.venv\Scripts\python.exe -m pytest tests\django -q`: 5 passed
- `.venv\Scripts\python.exe -m pytest tests\unit -q`: 351 passed, 26 skipped
- `.venv\Scripts\python.exe -m pytest tests\integration -q`: 15 passed
- `.venv\Scripts\python.exe -m pytest -q`: 393 passed, 27 skipped
- `.venv\Scripts\python.exe -m pip check`: 의존성 충돌 없음

### Not Verified

- 실제 MySQL 계정 이관, `audit_legacy_accounts`, reverse proxy/Ingress 경로 라우팅과 브라우저 E2E를 검증하지 않았다.

### Issues / Blockers

- 실제 배포 경로 라우팅 구성이 저장소에 없으므로 `/api/auth/*`와 `/admin/*`의 Django 전환은 배포 담당 검토가 필요하다.
- 기존 FastAPI 인증 코드는 최소 관찰 기간 동안 롤백용으로 남아 있으며 단계 5에서 제거해야 한다.

### Decisions

- 로그아웃·비활성화·역할 변경을 즉시 반영하기 위해 단기 서명 토큰 대신 Django 내부 인증 확인 API를 선택했다.
- 애플리케이션 `admin` 역할은 Django `is_staff`·`is_superuser`를 자동 부여하지 않는다.
- Django 5.2 LTS와 별도 `AUTH_INTROSPECTION_KEY`를 설정 계약으로 사용한다.
- 기존 `app/vectordb/qdrant_store.py`의 직접 import와 import smoke 계약을 맞추기 위해 누락된 `qdrant-client` 직접 의존성을 추가한다.

### Next Steps

1. 의존성을 설치한 격리 환경에서 Django system check, migration plan과 전체 테스트를 실행한다.
2. 테스트 account DB에 migration을 적용하고 `audit_legacy_accounts` 결과를 확인한다.
3. 배포 담당과 단일 공개 주소의 경로 라우팅·rollback 절차를 검토한다.
4. 스테이징 관찰 뒤 legacy FastAPI 인증 모듈과 롤백 설정을 제거한다.

### Learnings

- 서버 세션 기반 서비스 분리에서는 세션 DB나 Django `SECRET_KEY` 공유보다 소유 서비스의 내부 확인 API가 즉시 폐기 의미를 보존하기 쉽다.
- Django 로그인은 CSRF 토큰을 회전하므로 UI는 로그인 성공 뒤 캐시한 CSRF 토큰을 폐기해야 한다.

## Session 2 — 정적 전체 검토 | INT-AUTH-REVIEW 구조 분리 보정

- 작성자: integration-team
- 역할: `integration`
- 상태: In progress
- 테스트 실행: 사용자 요청에 따라 보류

### Goal

- 분리 계획, 정본 계약, 구현을 다시 대조해 책임 누수와 운영 전환 결함을 수정한다.

### Findings and Changes

- `account_db`가 FastAPI/MCP의 `allowed_databases`에 남아 있던 책임 누수를 제거했다.
  계정 DB는 Django만 소유하며 admin/hr 사용자 컨텍스트에도 업무 DB 권한으로 노출하지 않는다.
- Django의 상대 `STATIC_URL`과 FastAPI `/static` 충돌을 제거했다. Admin asset은
  `/django-static/*`의 `collectstatic` 산출물로 제공하도록 경로 계약을 분리했다.
- `createsuperuser`가 `display_name`과 `role` 없이 잘못된 계정을 만들 수 있던 구성을
  `REQUIRED_FIELDS`로 보완했다. 애플리케이션 admin 역할과 Django staff 권한 분리는 유지한다.
- legacy migration이 임의 scrypt 파라미터·잘못된 필드를 그대로 받아들이고 naive
  `DATETIME`을 UTC로 단정하던 문제를 수정했다. 고정 해시 형식과 필드 범위를 검증하고
  `LEGACY_ACCOUNT_TIME_ZONE`을 적용하며, 위험한 부분 rollback 대신 irreversible migration으로 표시했다.
- 이관 감사에 `created_at/date_joined` 비교를 추가하고, 인증 트래픽 전에 실행해야 하는
  순서를 README와 계획에 명시했다.
- FastAPI 내부 인증 HTTP client가 환경 proxy를 사용하지 않게 하고 성공 응답 타입과
  opaque session ID 형식을 엄격히 검증하도록 보완했다.
- 로그인 입력 타입·크기를 엄격히 검증하고, UI가 logout 실패를 성공으로 표시하지 않도록 수정했다.
- 오래된 단일 FastAPI 실행법과 실제로 없는 API를 설명하던 README를 현재 이중 서비스
  실행·migration·경로 라우팅 절차로 교체했다.
- rollback은 현 코드에서 legacy 라우터를 켜는 방식이 아니라, 보존 DB와 검증된 이전
  배포 버전으로 되돌리는 방식임을 계획에 명시했다.
- 관찰 중 Django 계정 변경으로 legacy snapshot과 계정 집합·역할·비밀번호가 달라지는
  것을 막기 위해 Admin 계정 추가·삭제와 이관 계정 변경을 기본 잠금했다.

### Verification

- 코드·문서·환경 변수·역할 정책의 정적 대조만 수행했다.
- pytest, Django system check, migration check, 서버·브라우저 실행은 수행하지 않았다.
- Session 1의 `393 passed, 27 skipped`는 이번 수정 전 기준선이며 최신 변경의 통과 결과로
  간주하지 않는다.

### Remaining

1. 승인된 시점에 `.venv`에서 Django check, migration 정합성, 전체 pytest를 다시 실행한다.
2. 백업된 테스트 account DB에서 legacy 시간대 확인 후 migration과 감사를 수행한다.
3. `/django-static/*`를 포함한 실제 gateway 경로와 내부 API 비공개 정책을 검증한다.

## Session 3 — 문서 최신화 | INT-DOCS 분리 구조 정본 동기화

- 작성자: integration-team
- 역할: `integration`
- 상태: Completed (documentation)
- 테스트 실행: 문서 변경만 수행해 보류

### Changes

- README에 활성 진입점, 구조 분리 환경 변수, 두 서비스 실행법, 동일 origin 경로 라우팅,
  migration·rollback 순서와 남은 전환 작업을 정리했다.
- `architecture.md`와 `interface.md`에 고정 세션 만료, 내부 introspection payload·오류 매핑,
  active/legacy 인증 경계와 Admin rollback 잠금을 반영했다.
- `test-scenarios.md`에 Django/FastAPI ASGI 인증 흐름 테스트와 `.venv` 표준 명령을 추가했다.
- `performance.md`의 분리 전 benchmark 범위를 명시하고 `SessionStore`, 순차 `BOTH`, conda
  실행 설명을 현재 코드에 맞게 수정했다.
- 오래된 `docs/progress/PROGRESS.md` 중복 현황을 제거하고 루트 `PROGRESS.md`를 정본으로
  연결했다. 팀 테스트 가이드는 과거 측정 수치와 현재 실행법을 구분했다.

### Verification

- 현재 코드의 Django/FastAPI route, settings, shared role policy와 문서 표현을 정적으로
  대조했다.
- pytest, Django check, migration, 서버·브라우저 실행은 수행하지 않았다.

## Session 4 — Django UI source 이전 | INT-UI-01

- 작성자: integration-team
- 역할: `integration`
- 상태: In Progress (외부 gateway·브라우저 검증 보류)

### Changes

- `django_app/web/` 앱을 추가하고 사용자 루트를 Django `web/index.html` template으로
  제공하도록 구성했다.
- HTML은 `templates/web/`, CSS·JavaScript는 `static/web/`으로 옮겼으며 UI API 호출은
  동일 origin 상대 `/api/*` 계약을 유지했다.
- FastAPI의 `/`, `/chat.js`, `/style.css`, `/static` 제공을 제거해 채팅·문서·상태 API
  전용 경계로 축소했다.
- Chart.js는 전환 기간에 한해 `app/web/vendor/`에서 Django staticfiles가 수집한다.
- Django page/static 발견과 FastAPI UI 미제공 회귀 테스트를 추가·갱신했다.

### Verification

- `.venv\Scripts\python.exe django_app\manage.py check --settings=django_app.config.test_settings`:
  이상 없음.
- `.venv\Scripts\python.exe -m pytest tests\django\test_web_ui.py tests\unit\test_web_auth.py tests\unit\test_api.py tests\test_api.py -q`:
  32 passed, 기존 embedding API FutureWarning 1건.
- Django migration dry-run과 collectstatic dry-run: 이상 없음.
- `.venv\Scripts\python.exe -m pytest -q`: 401 passed, 27 skipped, 기존 embedding API
  FutureWarning 1건.

### Remaining

1. 실제 gateway에서 `/`·인증·FastAPI API·`/django-static/*` 경로를 동일 origin으로 전환한다.
2. 실제 브라우저에서 로그인·채팅·표·차트·문서 다운로드·모바일·접근성을 검증한다.
3. Chart.js를 Django `web/static/web/vendor/`로 옮기고 legacy staticfiles 호환 설정을 제거한다.

## Session 5 — 로컬 Django UI 정적 자산 복구 | INT-UI-02

- 작성자: integration-team
- 역할: `integration`
- 상태: Completed

### Cause and fix

- `DJANGO_DEBUG=false`에서 일반 `runserver`나 ASGI 서버는 staticfiles source를 자동 제공하지
  않아 HTML은 200이지만 CSS·JavaScript·Chart.js가 404가 되는 원인이었다.
- `DJANGO_SERVE_STATIC_FILES=true`인 로컬 환경에서만 finder 기반 개발용 정적 route를
  등록했다. 운영은 `false`로 두고 collectstatic 산출물/CDN을 사용한다.
- `.env.example`, README, interface와 Django UI 테스트를 새 설정 계약에 맞췄다.

### Verification

- `--insecure` 없이 실행한 로컬 Django에서 `/`, CSS, JavaScript, Chart.js 모두 200.
- `.venv\Scripts\python.exe -m pytest tests\django -q`: 11 passed.
- `.venv\Scripts\python.exe -m pytest -q`: 402 passed, 27 skipped, 기존 FutureWarning 1건.

## Session 6 — 로컬 origin gateway 구현 | INT-GATEWAY-01

- 작성자: integration-team
- 역할: `integration`
- 상태: In Progress (Nginx 기동·브라우저 검증 보류)

### Changes

- `deploy/nginx/local.conf`에 공개 `127.0.0.1:8000`, Django upstream `:8001`, FastAPI
  upstream `:8002`의 경로 기반 reverse proxy를 구성했다.
- `/internal/auth/*`는 고정 `404`로 차단하고 알려지지 않은 `/api/*`도 Django HTML로
  fallback하지 않도록 했다.
- `/django-static/*`는 프로젝트 루트를 Nginx prefix로 사용해 `staticfiles/`의
  `collectstatic` 산출물을 제공하도록 구성했다.
- forwarded header를 gateway가 덮어쓰고 채팅 timeout, 요청 크기 제한과 문서 다운로드
  buffering 정책을 설정했다.
- README와 `.env.example`을 gateway 기준 실행 포트와 `DJANGO_SERVE_STATIC_FILES=false`
  계약으로 갱신했다.

### Verification

- `.venv\Scripts\python.exe django_app\manage.py check`: 이상 없음.
- `.venv\Scripts\python.exe django_app\manage.py collectstatic --noinput`: 130개 정적 파일 수집.
- `git diff --check`: whitespace 오류 없음. 기존 working tree 파일의 LF→CRLF 경고만 확인.
- 현재 개발 환경에서 `nginx` 실행 파일을 찾지 못해 `nginx -t`, proxy 기동과 브라우저
  동일 origin 흐름은 검증하지 못했다.
- Nginx 설치 후 수행할 설정 검사, 시작·reload·종료 명령을 README에 기록했다.

### Remaining

1. Nginx 설치 후 프로젝트 루트를 prefix로 지정해 설정 문법을 검사한다.
2. Django `:8001`, FastAPI `:8002`, gateway `:8000`을 함께 실행한다.
3. UI·인증·채팅·문서·정적 파일과 `/internal/auth/*` 차단을 실제 브라우저에서 검증한다.

## Session 7 — 단일 로컬 실행 launcher | INT-GATEWAY-02

- 작성자: integration-team
- 역할: `integration`
- 상태: In Progress (브라우저 인증·채팅 회귀 검증 보류)

### Changes

- `scripts/local_gateway.ps1` 하나로 Django check·collectstatic, Nginx 설정 검사와 세
  프로세스 시작을 수행하도록 구성했다.
- 같은 파일의 `status`, `restart`, `stop` action으로 전체 로컬 수명주기를 관리한다.
- Django와 FastAPI PID뿐 아니라 시작 시각을 기록해 재사용된 PID의 다른 프로세스를
  종료하지 않도록 했고 Nginx PID도 실제 프로세스 이름을 확인한다.
- Django와 FastAPI는 자동 reload 없이 숨김 프로세스로 실행하고 각 stdout/stderr를
  별도 runtime log에 기록한다.
- README의 세 터미널 수동 실행 절차를 단일 launcher 사용법으로 교체했다.

### Verification

- PowerShell parser 구문 검사: 오류 없음.
- launcher `stop → start → status → HTTP smoke → stop → status` 전체 흐름 확인.
- Django system check 이상 없음, collectstatic 130개 파일 변경 없음.
- `nginx -t`: 설정 문법과 경로 검사 성공.
- gateway `/`, `/api/health`, `/django-static/web/style.css`: 각각 `200`과 올바른 content type.
- gateway `/internal/auth/introspect`: `404`로 외부 차단 확인.
- 종료 후 Django `:8001`, FastAPI `:8002`, Nginx `:8000` listen socket이 남지 않음을 확인.
- 기존 Nginx가 관리자 권한으로 실행돼 첫 graceful quit이 거부됐으나, 승인된 관리자
  실행으로 해당 프로젝트 PID의 Nginx만 종료한 뒤 일반 권한 전체 흐름을 검증했다.

## Session 8 — Windows PowerShell 5.1 launcher 호환성 수정 | INT-GATEWAY-03

- 작성자: integration-team
- 역할: `integration`
- 상태: Completed

### Cause and fix

- `scripts/local_gateway.ps1`이 BOM 없는 UTF-8이어서 Windows PowerShell 5.1이 시스템 ANSI
  코드페이지로 해석했고, 한글 상태 문자열이 깨지면서 문자열 종결자 ParserError가
  발생했다.
- launcher 내부 문자열을 ASCII로 제한해 BOM이나 시스템 코드페이지와 무관하게
  PowerShell 5.1과 PowerShell 7에서 동일하게 파싱되도록 수정했다.
- 시작 시 해석한 `nginx.exe` 절대경로를 runtime 상태에 저장해, 이후 별도 PowerShell의
  PATH에서 Nginx를 찾지 못해도 `stop`과 `restart`가 같은 실행 파일을 사용하도록 했다.

### Verification

- Windows PowerShell `5.1.26100.9168`에서 ParserError 없이 `status` 실행.
- PowerShell 5.1에서 launcher 전체 `start`, Nginx 설정 검사와 세 서비스 기동 확인.
- gateway UI·health·CSS `200`, 내부 introspection `404` 확인.
- Nginx 경로를 PATH에 추가하지 않은 별도 PowerShell 5.1 프로세스에서 `stop` 성공.
- 종료 후 Django/FastAPI/Nginx 상태가 모두 `stopped`이고 `8000`·`8001`·`8002` listen
  socket이 남지 않음을 확인.

## Session 9 — 동일 origin 브라우저 부분 검증 | INT-GATEWAY-04

- 작성자: integration-team
- 역할: `integration`
- 상태: In Progress (승인된 테스트 계정 필요)

### Verification

- `http://127.0.0.1:8000/`에서 Django 로그인 화면과 UI 정적 자산 로드 확인.
- 존재하지 않는 계정 로그인 시 브라우저 alert에 기존 공개 메시지
  `아이디 또는 비밀번호가 올바르지 않습니다.` 표시 확인.
- 공개 gateway `http://127.0.0.1:8000/internal/auth/introspect`가 Nginx `404`를 반환해
  내부 인증 endpoint를 외부로 전달하지 않음을 확인.

### Remaining

1. 승인된 비민감 테스트 계정으로 로그인, `/api/auth/me`, 채팅, 로그아웃과 로그아웃 후
   `401` 흐름을 브라우저에서 확인한다.
2. 문서 다운로드, 표·차트와 오류·부분 실패 UI 회귀를 확인한다.

## Session 10 — 동일 origin 인증·채팅 성공 흐름 검증 | INT-GATEWAY-05

- 작성자: integration-team
- 역할: `integration`
- 상태: Completed (성공 흐름 범위)

### Verification

- 승인된 로컬 테스트 계정으로 `http://127.0.0.1:8000/`에서 로그인하고 관리자 사용자
  정보가 UI에 표시되는지 확인했다.
- 새로고침 뒤에도 로그인 상태가 복원되는 것을 확인했다.
- 동일 origin UI에서 FastAPI 채팅 API를 호출해 응답이 대화 영역에 렌더링되는 것을
  확인했다.
- 로그아웃 뒤 로그인 화면으로 전환되고, 새로고침 뒤에도 로그인 화면이 유지되는 것을
  확인했다.
- 브라우저가 JSON endpoint의 직접 탭 탐색을 차단해 로그아웃 후 `/api/auth/me`의 `401`
  본문은 별도 확인하지 못했다. UI의 세션 복원 동작으로 로그인 화면 유지 여부를
  확인했다.

### Remaining

1. CSRF 실패·세션 만료의 브라우저 흐름과 `/api/auth/me` HTTP `401`을 계약 수준에서
   확인한다.
2. 문서 다운로드, 표·차트, 오류·부분 실패와 Nginx rollback을 검증한다.

## Session 11 — Nginx gateway rollback 검증 | INT-GATEWAY-06

- 작성자: integration-team
- 역할: `integration`
- 상태: Completed

### Verification

- 정상 상태에서 gateway `/api/health`가 `200`인지 확인했다.
- Nginx 설정에 health endpoint의 의도적 `503` 응답을 일시 적용하고 `nginx -t` 뒤
  graceful reload해 장애 설정이 실제로 반영되는지 확인했다.
- 원본 설정으로 복구한 뒤 다시 `nginx -t`와 graceful reload를 수행했다.
- 복구 후 `/`, `/api/health`, `/django-static/web/style.css`는 `200`,
  `/internal/auth/introspect`는 `404`임을 확인했다.
- Django·FastAPI 코드와 DB migration은 변경하거나 되돌리지 않았다.

## Session 12 — CSRF·세션 만료 브라우저 검증 | INT-AUTH-UI-01

- 작성자: integration-team
- 역할: `integration`
- 상태: Completed

### Verification

- CSRF token 없이 gateway `/api/auth/login`에 JSON POST하면 `403`을 반환하는지 확인했다.
- 인증 없이 gateway `/api/chat`에 요청하면 `401`을 반환하는지 확인했다.
- 테스트 프로세스에만 `AUTH_SESSION_EXPIRE_SECONDS=2`를 주입해 로그인 직후 UI가 정상
  사용자 상태로 전환되는지 확인했다.
- 2초 경과 후 새로고침하면 로그인 화면이 표시되고 채팅 전송 UI가 사라지는 것을
  확인했다.
- 테스트 종료 뒤 launcher로 모든 로컬 프로세스를 종료했다. `.env` 값과 영구 설정은
  변경하지 않았다.

## Session 13 — UI XSS 경계·CSP 적용 | INT-UI-SEC-01

- 작성자: integration-team
- 역할: `integration`
- 상태: Completed (CSP/URL 경계 범위)

### Changes

- Django `web` namespace의 HTML 응답에만 Content-Security-Policy를 적용해 Admin의 별도
  inline 자산 계약을 변경하지 않았다.
- `script-src`, `connect-src`, `form-action`을 same-origin으로 제한하고 frame embed를
  차단했다. 동적 textarea 높이 조절 때문에 `style-src 'unsafe-inline'`은 유지했다.
- 웹 출처 링크는 `http`·`https` URL만, 문서 다운로드 URL은 same-origin
  `/api/documents/download`만 UI에 렌더링하도록 제한했다.

### Verification

- `.venv\Scripts\python.exe -m pytest tests\django\test_web_ui.py -q`: 5 passed.
- `.venv\Scripts\python.exe -m pytest tests\django -q`: 13 passed.
- Django test settings system check: 이상 없음.
- gateway UI 응답에서 CSP, `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`
  header를 확인했고 브라우저 console의 CSP 오류 없이 로그인 화면이 로드되는지 확인했다.

## Session 14 — Chart.js Django 정본 이전 | INT-UI-STATIC-01

- 작성자: integration-team
- 역할: `integration`
- 상태: Completed

### Changes

- Chart.js bundle을 `django_app/web/static/web/vendor/`로 이동하고 template static URL을
  `/django-static/web/vendor/chart.umd.min.js`로 변경했다.
- 전환용 `app/web/` 디렉터리와 `STATICFILES_DIRS` 호환 설정을 제거했다.
- 정적 파일·template 계약 테스트와 아키텍처·소유권·구조 분리 문서를 Django 단일 UI
  source 경계에 맞춰 갱신했다.

### Verification

- Django test settings system check 및 `makemigrations --check --dry-run`: 이상 없음.
- `.venv\Scripts\python.exe -m pytest tests\django\test_web_ui.py tests\unit\test_web_auth.py -q`:
  12 passed.
- `collectstatic --clear --noinput` 뒤 이전 `staticfiles/vendor/chart.umd.min.js`는 없고,
  새 `staticfiles/web/vendor/chart.umd.min.js`만 존재하는지 확인했다.
- gateway에서 새 Chart.js URL은 `200`, 이전 URL은 `404`이고 브라우저 console 오류 없이
  로그인 화면이 로드되는지 확인했다.

## Session 15 — 정적 fingerprint·cache 정책 | INT-UI-STATIC-02

- 작성자: integration-team
- 역할: `integration`
- 상태: Completed

### Changes

- 운영 Django 설정에 `ManifestStaticFilesStorage`를 적용해 CSS·JavaScript·Chart.js의
  내용 hash URL과 `staticfiles.json` manifest를 생성하도록 했다.
- Django HTML shell은 `no-cache`를 유지하고, Nginx의 `/django-static/` 응답에는
  `Cache-Control: public, max-age=31536000, immutable`을 설정했다.
- 로컬 gateway launcher가 시작 전 `collectstatic --clear --noinput`을 실행하도록 해
  더 이상 참조되지 않는 이전 번들을 제거한다. 테스트 설정은 manifest 산출물 없이 원본
  정적 source를 검증하도록 별도 storage backend를 사용한다.

### Verification

- `.venv\Scripts\python.exe -m pytest tests\django\test_web_ui.py tests\unit\test_web_auth.py -q`:
  13 passed.
- Django test settings의 `check`, `makemigrations --check --dry-run`을 통과했고
  `git diff --check`도 통과했다.
- `collectstatic --clear --noinput`으로 130개 파일을 수집·후처리해 manifest와
  hash된 `web/style`, `web/chat`, `web/vendor/chart` 자산을 확인했다.
- 운영 Django HTML이 hash된 세 자산 URL을 참조하고 HTML cache policy가
  `max-age=0, no-cache, no-store, must-revalidate, private`인지 확인했다.

## Session 16 — Django UI·FastAPI API 동일 origin 계약 | INT-UI-API-01

- 작성자: integration-team
- 역할: `integration`
- 상태: Completed

### Changes

- Django에서 실제 로그인으로 발급한 서버 세션을 FastAPI fake application에 전달해,
  채팅과 문서 다운로드가 같은 세션 인증 경계를 통과하는 통합 테스트를 추가했다.
- Nginx 설정에서 `/api/auth/*`는 Django, `/api/chat`·`/api/documents/*`는 FastAPI로
  우선 전달되고 그 밖의 `/api/*`는 fallback하지 않는 계약을 테스트로 고정했다.

### Verification

- `.venv\Scripts\python.exe -m pytest tests\integration\test_django_fastapi_auth_flow.py tests\django\test_web_ui.py tests\unit\test_web_auth.py -q`:
  17 passed.
- Django test settings의 `check`, `makemigrations --check --dry-run` 및
  `git diff --check`를 통과했다.
- MCP·DB·외부 HTTP는 사용하지 않았으며, 문서 MCP와 LLM은 결정적 fake로 대체했다.

## Session 17 — 표·차트·문서·오류 UI 회귀 | INT-UI-REGRESSION-01

- 작성자: integration-team
- 역할: `integration`
- 상태: Completed

### Changes

- UI가 `PARTIALLY_SUPPORTED`, `INSUFFICIENT`, `CONTRADICTED` 근거 상태를 코드값 대신
  사용자가 이해할 수 있는 안내로 표시하도록 보완했다.
- table·Chart.js·문서 다운로드·세션 만료·네트워크 오류·문자열 escaping의 UI 계약 테스트를
  추가하고, 기존 fake MCP 통합 테스트로 API table·문서·오류 응답 경계를 함께 검증했다.

### Verification

- `.venv\Scripts\python.exe -m pytest tests\django\test_web_ui.py tests\integration\test_chat_document_flow.py tests\integration\test_chat_data_flow.py tests\integration\test_chat_error_flow.py tests\integration\test_django_fastapi_auth_flow.py -q`:
  20 passed.
- local gateway의 로그인 UI shell이 정상 렌더링되고 브라우저 console 오류가 없음을 확인했다.
- 외부 MCP·DB는 호출하지 않았다. 브라우저 로그인은 비밀번호 입력이 필요한 실제 사용자
  동작이므로 자동화하지 않았고, 로그인 이후 API 흐름은 Django 세션과 결정적 fake로 검증했다.

## Session 18 — UI 보안 회귀 | INT-UI-SEC-02

- 작성자: integration-team
- 역할: `integration`
- 상태: Completed

### Changes

- 세션·CSRF 쿠키의 `HttpOnly`/`SameSite`/환경별 `Secure` 계약과 내부 인증 key 비노출을
  Django API 테스트에 추가했다.
- CSP의 script/connect/object/frame 제한, same-origin template 자산, HTML·SQL·문서 발췌
  escaping 및 text-only 오류 표시를 UI 계약 테스트로 고정했다.

### Verification

- `.venv\Scripts\python.exe -m pytest tests\django\test_auth_api.py tests\django\test_web_ui.py tests\integration\test_django_fastapi_auth_flow.py -q`:
  20 passed.
- Django test settings의 `check`, `makemigrations --check --dry-run` 및
  `git diff --check`를 통과했다.
- local gateway에서 CSP, `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`,
  `Referrer-Policy: same-origin` 및 `/internal/auth/introspect`의 `404`를 확인했다.
- 브라우저 UI shell은 외부 자산 없이 로드됐고 console error가 없었다. 실제 로그인은
  비밀번호 입력이 필요한 사용자 동작이라 자동화하지 않았다.

## Session 19 — legacy 인증 경계 제거 사전 점검 | INT-LEGACY-CLEANUP-01

- 작성자: integration-team
- 역할: `integration`
- 상태: Completed

### Findings

- `app/api/auth.py`, `app/auth/{repository,service,sessions,session_store}.py`는 활성
  FastAPI app에 등록·import되지 않으며 rollback 전용 legacy source다.
- 활성 `AppDependencies`는 Django 내부 인증 확인 gateway만 사용한다. legacy source를
  직접 참조하는 대상은 legacy 전용 테스트와 과거 문서뿐이다.
- `LEGACY_AUTH_ROLLBACK_WINDOW`과 legacy `accounts` 원본은 관찰 기간 중 Django 계정
  집합과 로그인 rollback 가능성을 보장하는 안전장치다.

## Session 20 — FastAPI legacy 인증 경계 제거 | INT-LEGACY-CLEANUP-02

- 작성자: integration-team
- 역할: `integration`
- 상태: Completed (사용자 승인)

### Changes

- FastAPI의 legacy account DB repository, 로그인 route, scrypt 비밀번호 유틸리티,
  서명 세션 registry와 전용 schema를 제거했다.
- legacy account 시드 스크립트·빈 SQL template·통합 초기화 script 및 FastAPI 전용
  환경 변수를 제거했다. 신규 Django 계정은 `createsuperuser` 또는 Django Admin으로 관리한다.
- Django Admin의 rollback 관찰 잠금과 `LEGACY_AUTH_ROLLBACK_WINDOW` 설정을 제거했다.
  기존 계정의 첫 로그인 재해싱과 irreversible data migration은 보존했다.

### Verification

- `.venv\Scripts\python.exe -m pytest tests\django\test_auth_api.py tests\integration\test_django_fastapi_auth_flow.py tests\unit\test_auth.py tests\unit\test_api.py -q`:
  33 passed.
- Django test settings의 `check`, `makemigrations --check --dry-run`을 통과했고,
  legacy import·설정·시드 사용처가 활성 source에 남지 않는지 확인했다.
- local gateway 실제 실행에서 `/`, `/api/health`, hash된 UI 정적 자산, Django 로그인·세션
  복원·로그아웃·로그아웃 후 `401`, 내부 인증 경로 `404`, 인증된 `/api/chat`의 `200`을
  확인했다.

## Session 21 — 안전한 통합 초기화 도구 | INT-SETUP-ALL-01

- 작성자: integration-team
- 역할: `integration`
- 상태: Completed

### Changes

- `setup_all.py`가 Django 설정 검사·migration, 문서 경로 등록·FAISS 인덱싱, 구매·판매
  ETL을 순서대로 조립한다. 기본 실행은 계획 출력이며 실제 변경은 `--apply`가 필요하다.
- `.env` 존재와 선택한 ETL workbook을 사전 검사하고, 문서·도메인 단계 생략 및 대화형
  `createsuperuser` 실행 옵션을 제공한다. `.env` 생성·수정과 임의 자격 증명·DB SQL 실행은
  수행하지 않는다.

### Verification

- `.venv\Scripts\python.exe -m pytest tests\unit\test_setup_all.py tests\unit\test_auth.py tests\django\test_auth_api.py -q`:
  15 passed.
- `.venv\Scripts\python.exe setup_all.py`가 데이터 변경 없이 6개 초기화 단계와 `--apply`
  요구 사항을 출력했다.
