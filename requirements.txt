# FastAPI 웹 API 프레임워크입니다.
fastapi>=0.115,<1.0

# FastAPI ASGI 서버입니다.
uvicorn[standard]>=0.34,<1.0

# .env 기반 설정 관리를 제공합니다.
pydantic-settings>=2.7,<3.0

# OpenAI Responses 및 Embeddings API 클라이언트입니다.
openai>=1.68,<3.0

# 공식 MCP Python SDK입니다.
mcp[cli]>=1.2,<2.0

# CPU 환경에서 사용할 수 있는 FAISS 패키지입니다.
faiss-cpu>=1.9,<2.0

# Qdrant local/server 클라이언트입니다.
qdrant-client>=1.13,<2.0

# 벡터 배열 계산에 사용합니다.
numpy>=1.26,<3.0

# MySQL 연결 드라이버입니다.
mysql-connector-python>=9.1,<10.0

# PDF에서 텍스트를 추출합니다. (docs 폴더의 규정 PDF 처리에 필요)
pypdf>=5.0,<7.0

# pytest 테스트 프레임워크입니다.
pytest>=8.3,<10.0

# FastAPI TestClient(httpx 기반) 실행에 필요합니다.
httpx>=0.28,<1.0

# 선택 사항: EMBEDDING_BACKEND=sbert로 설정하면 실제 의미 기반 로컬 임베딩을 사용합니다.
# (최초 실행 시 huggingface에서 모델을 한 번 다운로드합니다. 인터넷 필요, 이후엔 오프라인 동작)
# sentence-transformers

# 선택 사항: 한국어 PDF에서 흔한 "띄어쓰기 소실" 문제를 교정합니다.
# 설치하지 않아도 RAG는 동작하지만, 검색/스니펫 가독성이 떨어질 수 있습니다.
# pip install git+https://github.com/haven-jeon/PyKoSpacing.git
# pykospacing
